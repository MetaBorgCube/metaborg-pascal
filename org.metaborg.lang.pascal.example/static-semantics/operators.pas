program Sort();
var 
  x : integer;
  
procedure f(var x : boolean);
  var y : integer;
      z : integer;
  begin 
    x := true;  
    x := x and true; 
    x := x and 1;
    y := y mod 3;
    z := 1
  end;
  
begin 
  x := x + 7
end.
