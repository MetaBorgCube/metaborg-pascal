program example1();

  $FunctionHeading;
     $Block;

  $FunctionIdentification;
       $Block;

  function $Identifier $FormalParameters : $ResultType;
    label
      $Integernumber;
    const
      $Identifier = $Constant;   
    type
      $Identifier = (a);
    procedure $Identifier $FormalParameters;
    forward;
    
  begin
    
  end;
  
  
  
begin
  inc(3);
    while $Expression + 3 do
      $Variable := $Expression
end.   

